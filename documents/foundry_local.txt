Foundry Local runs artificial intelligence models directly on a user's device. It does not require a cloud account or an API key.

Foundry Local downloads, loads, and manages optimized language models. It can use CPU, GPU, or NPU hardware acceleration.